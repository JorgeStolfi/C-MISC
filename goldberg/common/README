Creates common code used by multiple implementations

base_parser.cc		used to parse push-relabel implementations
parse_arguments.cc	used to parse push-relabel implementations
timer.c			timer used in ALL implementations
