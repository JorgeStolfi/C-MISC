
#include <Force.h>


 
REVEAL
  T == Public BRANDED OBJECT OVERRIDES
      compute = Compute;
    }

PROCEDURE Compute(<*UNUSED, "??"); T fo,
                  <*UNUSED, "??"); pos, vel, Vector f,
                  <*UNUSED, "??"); double t) == 
{; } /* Compute */;

{; } /* Force */.
